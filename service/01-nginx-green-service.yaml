apiVersion: v1
kind: Service
metadata:
  name: nginx-green-svc
  namespace: homelab
spec:
  selector:
    app: nginx-green
  type: LoadBalancer

  ports:
    - name: nginx-green-svc
      protocol: TCP
      port: 80
      targetPort: 80
